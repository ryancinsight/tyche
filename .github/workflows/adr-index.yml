name: ADR index

on:
  push:
    branches: [main]
    paths:
      - 'docs/adr/**'
      - '.github/workflows/adr-index.yml'
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, converted_to_draft]
    paths:
      - 'docs/adr/**'
      - '.github/workflows/adr-index.yml'

permissions:
  contents: read

concurrency:
  group: adr-index-${{ github.ref }}
  cancel-in-progress: true

jobs:
  index:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
    name: ADR index is current
    # atlas owns the one generator (ATLAS-ADR-FORM-NORMALIZATION-2026-09-02); strict: the ADRs carry the canonical heading form and numbering.
    uses: ryancinsight/atlas/.github/workflows/adr-index-guard.yml@6663f52f17682c0f1bcf0f4fe41952911c64440a
    permissions:
      contents: read
    with:
      strict: true
